CREATE TABLE IF NOT EXISTS system_settings(setting_key VARCHAR(120) PRIMARY KEY,setting_value TEXT NULL,secret_ciphertext TEXT NULL,is_secret BOOLEAN NOT NULL DEFAULT FALSE,updated_by BIGINT UNSIGNED NULL,updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,FOREIGN KEY(updated_by) REFERENCES users(id) ON DELETE SET NULL) ENGINE=InnoDB;
